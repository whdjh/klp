import { View } from "react-native";

export default function Dashboard() { 
  return (
    <View className="flex-1 bg-black justify-center text-white">
      글 목록 리스트
      글 클릭하면 상세
      글작성버튼
    </View>
  );
}